// Program 3: Reverse traversal in a singly linked list

#include <stdio.h>
#include <stdlib.h>

// Node definition for singly linked list
typedef struct Node {
    int data;
    struct Node *next;
} Node;

int main(void) {
    return 0;
}